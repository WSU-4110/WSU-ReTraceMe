-- CREATE DATABASE locater;
/*
database already created
Database set as a skeleton for the waypoints
CREATE TABLE loc (
    id int auto_increment PRIMARY KEY,
    Longitude varchar(50),
    Latitude varchar(50),
    timestamp datetime
);
*/
use locater;
select * from waypoints;